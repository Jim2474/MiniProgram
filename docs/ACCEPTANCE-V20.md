# V20 验收记录

日期：2026-06-08

## 本轮目标

- 修复 V17 桌台图片 seed 数据落点错误。
- 加强预约桌台图片字段验收，避免空图片通过。

## 已完成

- 商品 `sku_bud` 图片恢复为啤酒占位图。
- 商品 `sku_whisky` 图片恢复为威士忌占位图。
- 桌台 `table_a1` 图片配置为 A1 德扑桌占位图。
- 桌台 `table_vip` 图片配置为 VIP 房台占位图。
- 已有数据加载时若桌台 `imageUrl` 为空，会自动补默认桌台图。
- 后台新增/编辑桌台时若未填写图片，会返回默认桌台图，避免客户预约页空图。
- 后端自测改为要求客户预约页返回的桌台 `imageUrl` 非空。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：115 项通过。
- 小程序结构与 API 调用校验：111 项通过。

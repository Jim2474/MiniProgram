# V59 验收记录

日期：2026-06-08

## 目标

继续去除后台演示式固定操作，把订单转存客户存酒从“固定威士忌 1 瓶”改为按后台表单选择 SKU 和数量。

## 已完成

- 后台订单管理新增“订单转存表单”。
- 表单支持从 SKU 列表选择转存商品。
- 表单支持输入转存数量。
- 订单行“转存威士忌”按钮改为“按表单转存”。
- 小程序提交 `/api/admin/orders/:orderId/transfer-storage` 时使用表单中的 `skuId` 和 `quantity`。

## 自动验收

```bash
npm test
```

通过项：

- 后端业务自验收：189 项。
- 小程序结构与 API 调用校验：157 项。

## 剩余风险

- 微信开发者工具模拟器仍需人工确认 SKU picker、数量输入和订单转存按钮体验。
- 当前转存数量仍由后端校验是否大于 0；后续可增加前端即时校验和订单内可转存 SKU 限制。

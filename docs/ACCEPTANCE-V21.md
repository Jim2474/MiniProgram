# V21 验收记录

日期：2026-06-08

## 本轮目标

- 降低当前 JSON Store 在单进程部署下的并发写竞态。
- 降低保存过程中异常中断造成数据文件损坏的风险。
- 为库存前置校验增加并发边界自测。

## 已完成

- API 路由层增加请求串行队列，handler 执行和 `store.save()` 按顺序完成。
- `Store.save()` 改为写入临时文件后原子替换正式数据文件。
- 数据目录按实际 `dataFile` 自动创建，支持测试/部署指定自定义数据文件位置。
- 新增并发加购低库存 SKU 自测，同一库存 1 的 SKU 两个请求同时加购时，只允许一个成功。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：116 项通过。
- 小程序结构与 API 调用校验：111 项通过。

## 仍需生产化

- 当前队列只覆盖单 Node.js 进程；多进程/多实例部署仍需数据库事务或分布式锁。
- 正式上线前仍建议替换为数据库，并为订单、支付、库存流水、积分流水、客户存酒账增加事务、索引、备份和迁移。

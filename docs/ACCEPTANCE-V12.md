# V12 验收记录

日期：2026-06-08

## 本轮目标

- 补齐客户侧“今日订单”“历史订单”“历史存取记录”。
- 让客户能看到取酒申请的历史状态，不只让后台看到流水。

## 已完成

- 新增后端 `/api/storage-records` 客户侧历史存取记录接口。
- `/api/storage-records` 返回客户存酒流水和取酒申请记录。
- 客户端订单列表拆分为“今日订单”和“历史订单”。
- 客户端存酒区新增“历史存取记录”。
- 客户端存酒区新增“取酒申请记录”，展示待确认、已完成、已拒绝等状态。
- 自动验收覆盖客户查询历史存取酒流水和取酒申请状态。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：114 项通过。
- 小程序结构与 API 调用校验：106 项通过。

额外本地验证：

- 启动后端后访问 `GET /api/health`，返回 `{"ok":true}`。

## 仍需生产接入

- 当前仍是模拟微信登录、模拟手机号授权、模拟微信支付与模拟退款。
- 上线前仍需接入真实微信登录、支付回调、数据库、鉴权会话与小程序真机联调。

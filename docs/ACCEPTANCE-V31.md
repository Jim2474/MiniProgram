# V31 验收记录

日期：2026-06-08

## 本轮目标

- 将员工点单码、客户积分/存酒核销码从纯文本码值升级为可展示、可扫码的二维码图片。
- 保留原始码值作为开发者工具和人工排查兜底。

## 已完成

- 后端新增 `qrcode` 依赖，用现有 `employee:<employeeId>` 与 `verify:<codeId>` 生成二维码 data URI。
- `/api/staff/employees/:employeeId/order-qr` 返回 `qrImageUrl`。
- `/api/verification-codes` 创建积分/存酒核销码时保存并返回 `qrImageUrl`。
- 员工端“专属点单二维码”直接展示二维码图片，同时保留码值弹窗。
- 客户端生成积分/存酒核销码后展示二维码图片，历史核销码列表展示缩略图。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：156 项通过。
- 小程序结构与 API 调用校验：125 项通过。

新增覆盖：

- 员工专属点单二维码返回可展示图片。
- 客户取积分二维码返回可展示图片。
- 客户取酒二维码返回可展示图片。
- 客户页和员工页静态校验覆盖二维码图片展示。

## 剩余上线风险

- 当前二维码图片为服务端生成的 payload 二维码，不是微信小程序码；如需带小程序路径/scene 的小程序码，还需接入微信 `getUnlimitedQRCode` 等服务端能力。
- 微信开发者工具 GUI/真机仍需人工扫码回归。

# V89 验收记录

日期：2026-06-08

## 目标

补齐 Excel 后台升盲设置第 34 行“logo 可以上传该店家的 logo，在屏幕上显示”的展示闭环。此前后台只保存 Logo URL，荷官端也只把 Logo 当文字配置展示，没有在计时器现场画面中显示图片。

## 已完成

- 后台升盲设置在 Logo URL 下方新增 Logo 图片预览。
- 荷官端游戏进行中页面读取后台 Logo 配置并显示图片。
- Logo 图片使用固定尺寸 `aspectFit`，避免加载后挤压计时器布局。
- 小程序结构校验覆盖后台 Logo 预览和荷官端 Logo 显示。

## 自动验收

```bash
npm test
```

通过项：

- 后端业务自验收：232 项。
- 小程序结构与 API 调用校验：203 项。

## 剩余风险

- 当前仍沿用 URL 配置方式；正式“上传本地图片到对象存储/素材库”的文件服务、鉴权和域名白名单，需要随生产部署策略一起接入。

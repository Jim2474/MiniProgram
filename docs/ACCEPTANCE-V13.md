# V13 验收记录

日期：2026-06-08

## 本轮目标

- 落实“只按员工专属二维码归属，不按客户手动选择员工”。
- 客户未扫码时订单必须是自然订单，不应默认归属某个员工。

## 已完成

- 客户端移除员工手动选择 picker。
- 客户端默认不绑定员工，未扫码时购物车和订单均不带员工归属。
- 客户端扫码员工二维码后才保存 `scannedEmployeeId` 并绑定订单归属。
- 小程序全局默认 `selectedEmployeeId` 改为空，避免进入客户页自动归属测试员工。
- 员工端仍保留专属二维码参数展示，供员工出示给客户扫码。
- 自动验收覆盖未扫码时订单为自然订单。
- 自动验收覆盖先扫码员工二维码再下单时订单归属该员工。
- 静态校验覆盖客户页不再包含员工手动选择入口。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：115 项通过。
- 小程序结构与 API 调用校验：107 项通过。

额外本地验证：

- 启动后端后访问 `GET /api/health`，返回 `{"ok":true}`。

## 仍需生产接入

- 当前仍是模拟微信登录、模拟手机号授权、模拟微信支付与模拟退款。
- 上线前仍需接入真实微信登录、支付回调、数据库、鉴权会话与小程序真机联调。

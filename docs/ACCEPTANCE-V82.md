# V82 验收记录

日期：2026-06-08

## 目标

补齐 Excel 荷官端设置项：升盲间隔应提供 5、8、10、12、15、20 分钟选项，语音提醒应可开关且默认开启。此前小程序用手填分钟，且创建本局页面没有语音开关。

## 已完成

- 荷官端创建本局将升盲间隔改为固定 picker：5、8、10、12、15、20 分钟。
- 荷官端新增“语音提醒” switch，默认开启。
- 创建升盲游戏时提交 `intervalMinutes` 和 `voiceEnabled`。
- 加载已有游戏时同步当前间隔和语音开关状态。
- 后端自验收覆盖所选间隔保存、语音关闭保存，以及关闭语音后不生成单挑语音事件。
- 小程序结构校验覆盖固定间隔 picker 和语音提醒 switch。

## 自动验收

```bash
npm test
```

通过项：

- 后端业务自验收：230 项。
- 小程序结构与 API 调用校验：188 项。

## 剩余风险

- 微信开发者工具/真机仍需确认 picker 和 switch 在荷官端首屏的触达体验，尤其横屏或投屏场景。

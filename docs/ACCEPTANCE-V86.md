# V86 验收记录

日期：2026-06-08

## 目标

补齐 Excel 后台升盲设置第 41-42 行：语音类型和小盲/大盲/前注术语应像配置项一样选择，而不是完全手填。此前后台只有语音类型文本框和三个术语文本框。

## 已完成

- 后台升盲设置新增语音类型 picker：默认语音、自定义语音、静音提示。
- 后台升盲设置新增小盲术语 picker：小盲、半基础分、预留分。
- 后台升盲设置新增大盲术语 picker：大盲、基础分、空。
- 后台升盲设置新增前注术语 picker：前注、预留分、前置预留分。
- 加载后台配置时自动同步语音类型和三个术语 picker。
- 保留开始语音文案、结束语音文案输入框，继续支持自定义播报文案。
- 保存仍写入原有 `voiceType` 和 `voiceTerms` 结构。
- 小程序结构校验覆盖语音类型和三类术语 picker。

## 自动验收

```bash
npm test
```

通过项：

- 后端业务自验收：231 项。
- 小程序结构与 API 调用校验：199 项。

## 剩余风险

- 当前仅完成配置入口和字段保存；真实语音合成、音色资源和播放效果仍需要微信开发者工具/真机回归确认。

# V51 验收记录

日期：2026-06-08

## 目标

继续补齐 Excel 中“员工端座位/牌局操作”的真实业务输入，员工确认客户入座时不再固定使用演示会员，而是按客户手机号定位会员。当前仍保持“后台管理 + 客户预约申请 + 简化座位管理”，不引入复杂实时排台。

## 已完成

- 员工端“座位管理”新增入座客户手机号输入框。
- 员工点击“入座”时提交客户手机号和员工操作人。
- `/api/staff/seats/:seatNo/sit` 支持按手机号查找会员并写入座位归属。
- 后端继续兼容旧的 `userId` 入座调用，便于自动化和历史调试脚本使用。
- 员工端座位入座移除固定 `user_demo` 的演示式写法。

## 自动验收

```bash
npm test
```

通过项：

- 后端业务自验收：180 项。
- 小程序结构与 API 调用校验：150 项。

## 剩余风险

- 微信开发者工具模拟器仍需人工确认座位手机号输入、按钮点击和 toast 展示。
- 手机号采用精确匹配，后续如果接入微信手机号授权或会员搜索，应补充更友好的选择/校验体验。

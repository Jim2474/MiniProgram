# V6 后台配置闭环验收记录

验收时间：2026-06-08

## 本轮新增范围

### 商家后台

- 首页看板增加较昨日营收、订单、新增会员对比字段。
- 商品分类可编辑/启停。
- SKU 可编辑、上架、下架。
- 充值配置可停用。
- 会员等级可编辑/停用。
- 积分配置独立接口，支持签到积分、积分有效期、兑换酒水券积分数、积分显示开关。
- 抽奖设置的每日次数、冷却时间、消耗积分可配置。
- 抽奖冷却时间真实拦截。
- 抽奖奖品可编辑/停用。

### 小程序后台页

- 显示较昨日指标。
- 商品分类启停入口。
- SKU 上下架入口。
- 充值配置停用入口。
- 会员等级停用入口。
- 积分配置更新入口。
- 抽奖冷却与次数配置入口。
- 奖品停用入口。

## 自动验收

命令：

```bash
npm test
```

结果：通过。

### 后端测试

命令：

```bash
npm --prefix backend test
```

结果：通过，84 项检查。

本轮新增覆盖：

- 后台看板返回较昨日对比数据。
- 后台可停用充值配置。
- 后台可配置抽奖冷却和每日次数。
- 抽奖冷却时间生效。
- 后台可编辑会员等级。
- 后台可停用/启用商品分类。
- 后台可编辑并下架/上架 SKU。
- 后台可配置积分规则。
- 后台可编辑并停用抽奖奖品。

### 小程序结构与 API 调用测试

命令：

```bash
node scripts/validate-miniprogram.mjs
```

结果：通过，88 项检查。

本轮新增覆盖：

- 后台页调用分类更新接口。
- 后台页调用商品更新接口。
- 后台页调用充值配置更新接口。
- 后台页调用会员等级更新接口。
- 后台页调用积分配置接口。
- 后台页调用抽奖奖品更新接口。

## 本地运行验证

后端运行验证：

```text
GET http://localhost:3000/api/health -> {"ok":true}
```

## 微信开发者工具验证

当前进程检查：

- `wechatdevtools.exe` 进程中存在窗口标题：`Miniprogram - 微信开发者工具 Stable v2.01.2510290`。
- `WeChatAppEx.exe` 模拟器运行时进程存在。

说明：

- V3/V4/V5 中已多次尝试 DevTools CLI `preview`，均 30 秒超时且未生成机器可读预览文件。
- V6 未重复消耗在同一无响应 CLI 通道上，继续以 GUI 打开状态、后端健康检查、后端自测、小程序结构/API 校验作为本轮可自动取得的证据。

## Excel 覆盖说明

V6 把后台配置类要求推进为可编辑闭环：

- 商品分类、商品列表、上下架。
- 充值配置列表与启停。
- 会员等级配置。
- 积分配置：签到积分、有效期、兑换券积分数。
- 抽奖设置：开关、每日次数、冷却时间、消耗积分。
- 奖品管理：新增、编辑、停用。
- 首页今日数据较昨日对比。

仍需生产环境接入：

- 真实微信登录/手机号授权。
- 真实微信支付、退款和支付回调。
- 真实二维码生成与服务端扫码参数签名。
- 真实图片上传、语音播放和地图定位权限配置。

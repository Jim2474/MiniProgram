# V56 验收记录

日期：2026-06-08

## 目标

补齐 Excel 客户端“个人中心：登录信息、默认头像，未登录时显示登录按钮”的前端体验。

## 已完成

- 客户端个人中心新增登录态字段 `isLoggedIn`。
- 未登录时显示默认头像、未登录昵称、待绑定手机号和登录按钮。
- 登录成功后记录 `customerLoggedIn`，个人中心展示会员昵称、头像和手机号。
- 微信手机号授权成功后同样切换为已登录态。
- 保留本地演示会员 `user_demo` 作为开发态数据兜底，不影响现有点单、积分和存酒演示。

## 自动验收

```bash
npm test
```

通过项：

- 后端业务自验收：187 项。
- 小程序结构与 API 调用校验：154 项。

## 剩余风险

- 微信开发者工具模拟器仍需人工确认默认头像、登录按钮和登录后头像展示。
- 生产环境头像来自微信登录资料；若微信侧不返回头像，仍会显示默认头像。

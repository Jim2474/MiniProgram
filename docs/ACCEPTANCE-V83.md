# V83 验收记录

日期：2026-06-08

## 目标

补齐 Excel 后台升盲设置第 39 行“其他设置”：显示北京时间、显示截止报名倒计时、倒计时结束自动开始、显示接受/停止报名状态。此前后端已有字段，但后台表单只暴露报名状态文本框和自动开始开关。

## 已完成

- 后台升盲设置新增报名状态 picker：接受报名、停止报名。
- 后台升盲设置新增“显示北京时间”开关。
- 后台升盲设置新增“显示报名倒计时”开关。
- 保存升盲设置时提交 `showBeijingTime`、`showRegistrationCountdown` 和受控 `registrationStatus`。
- 加载后台配置时同步报名状态 picker 和两个显示开关。
- 后端自验收覆盖北京时间、报名倒计时和报名状态配置保存。
- 小程序结构校验覆盖报名状态 picker、北京时间开关和报名倒计时开关。

## 自动验收

```bash
npm test
```

通过项：

- 后端业务自验收：231 项。
- 小程序结构与 API 调用校验：191 项。

## 剩余风险

- 当前为配置保存与展示入口，微信开发者工具/真机仍需确认长后台表单滚动、picker 弹层和多个 switch 的操作体验。

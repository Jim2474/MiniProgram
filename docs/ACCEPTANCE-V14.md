# V14 验收记录

日期：2026-06-08

## 本轮目标

- 补齐“员工出示专属点单二维码 -> 客户扫码 -> 订单归属员工”的员工端出示环节。
- 让员工端展示的码值与客户扫码解析格式完全一致。

## 已完成

- 后端新增员工专属点单码接口 `/api/staff/employees/:employeeId/order-qr`。
- 接口返回统一码值 `employee:<employeeId>`、场景 `employee_qr`、员工安全信息和出示提示。
- 员工端加载当前员工的专属点单码，并在页面中展示。
- 员工端新增“出示码值”弹窗，便于开发者工具与真机扫码联调。
- 客户扫码归属接口校验员工 id 与码值一致，避免错配归属。
- 自动验收覆盖员工端获取专属点单码、接口不泄露密码字段、客户扫码使用同一专属码值下单归属。
- 小程序静态校验覆盖员工端调用点单码接口并展示 `orderQrPayload`。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：117 项通过。
- 小程序结构与 API 调用校验：109 项通过。

## 仍需生产接入

- 当前弹窗展示的是二维码码值，后续生产版需要渲染为真实二维码图片或接入小程序码生成服务。
- 当前仍是模拟微信登录、模拟手机号授权、模拟微信支付与模拟退款。
- 上线前仍需接入真实微信登录、支付回调、数据库、鉴权会话与小程序真机联调。

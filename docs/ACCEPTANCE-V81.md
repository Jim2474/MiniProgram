# V81 验收记录

日期：2026-06-08

## 目标

补齐 Excel 第 38 行“计时器名词标题自定义”。此前后端默认值已含完整标题，但后台表单只暴露了 LEVEL、PLAYER LEFT、BLINDS、ENTRANTS 四项，荷官端也只展示四项；本轮把完整标题配置和恢复默认标题按钮接到小程序。

## 已完成

- 后端抽出 `defaultBlindTitleMap`，种子数据和旧数据加载都使用同一套默认标题。
- 后台升盲设置支持编辑：级别、剩余人数、参赛人数、奖励人数、盲注、前注、下一级、下次休息、平均记分牌、总记分牌。
- 后台新增“恢复默认标题”按钮。
- 升盲设置保存时写入完整 `titleMap`。
- 荷官端展示完整名词标题配置，方便现场核对屏幕文案。
- 自动验收覆盖完整标题保存、后台表单字段、恢复默认按钮和荷官端展示。

## 自动验收

```bash
npm test
```

通过项：

- 后端业务自验收：228 项。
- 小程序结构与 API 调用校验：186 项。

## 剩余风险

- 当前仍是轻量配置面板，标题字段较多，微信开发者工具/真机上需要确认长页面滚动和输入体验是否顺手。

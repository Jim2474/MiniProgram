# V45 验收记录

日期：2026-06-08

## 本轮目标

- 让后台人员管理从固定“测试员工”按钮升级为真实员工新增表单。
- 继续满足员工提成和账号安全要求。

## 已完成

- 后台人员与系统设置区新增“新增员工表单”。
- 支持填写员工姓名、手机号、登录账号、初始密码、角色和提成率。
- 新增员工时使用表单值调用 `/api/admin/employees`。
- 移除固定“新增测试员工”的演示式操作。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：178 项通过。
- 小程序结构与 API 调用校验：142 项通过。

新增覆盖：

- 后台页支持真实员工新增表单。
- 后台页不再出现固定“新增测试员工”入口。
- 既有后端验收继续覆盖新增员工提成率、强哈希密码和密码字段不外泄。

## 剩余上线风险

- 微信开发者工具/真机仍需确认员工角色输入的易用性。
- 后续可把角色输入升级为 picker，避免手输 `staff/dealer/warehouse` 出错。

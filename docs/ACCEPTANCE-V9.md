# V9 验收记录

日期：2026-06-08

## 本轮目标

- 把后台 SKU 管理里的库存预警和存酒有效期配置暴露到小程序后台。
- 把咖位后台补成 Excel 要求里的搜索、状态筛选、分页和状态汇总。

## 已完成

- 新增 SKU 表单支持填写预警库存 `warningQty`。
- 新增 SKU 表单支持填写存酒有效期天数 `storageDays`，商品列表展示该配置。
- 后端 `/api/admin/tables` 支持 `keyword`、`status`、`page`、`pageSize` 查询参数。
- 后端 `/api/admin/tables` 返回 `pagination` 分页信息与 `summary` 状态汇总。
- 后台咖位管理展示总咖位、空闲、占用、预订、维护数量。
- 后台咖位管理支持按桌台名称/类型搜索、按状态筛选、上一页/下一页翻页。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：104 项通过。
- 小程序结构与 API 调用校验：100 项通过。

额外本地验证：

- 启动后端后访问 `GET /api/health`，返回 `{"ok":true}`。

## 仍需生产接入

- 当前仍是模拟微信登录、模拟手机号授权、模拟微信支付与模拟退款。
- 上线前仍需接入真实微信登录、支付回调、数据库、鉴权会话与小程序真机联调。

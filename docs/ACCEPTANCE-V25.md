# V25 验收记录

日期：2026-06-08

## 本轮目标

- 直接读取 `小程序进阶版C(1).xlsx` 后，补齐仍属于当前范围的 Excel 明细。
- 不恢复用户口头明确收敛掉的充值、抽奖、积分兑换和排行榜。

## 已完成

- 客户商品列表新增“查看详情”弹窗，展示规格、描述、价格和库存。
- 后台会员列表展示手机号、会员等级、积分、是否存酒、存酒数量、累计消费和订单数。
- 后台座台支持删除/禁用，采用软删除状态 `disabled`，保留历史预约引用和操作日志。
- 生产强制鉴权下，库房角色 `warehouse` 可访问出入库/库存接口，但不能访问经营看板。
- 新增 `/api/staff/blind-settings`，荷官端不再读取管理员接口。
- 升盲设置支持 `blindLevels` 自定义盲注序列。
- 荷官升盲按后台自定义盲注序列推进，并在荷官端展示升盲规则。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：132 项通过。
- 小程序结构与 API 调用校验：121 项通过。

## Excel 范围说明

- 充值中心、余额支付、积分排行榜、积分抽奖、积分兑换酒水券按用户后续口头要求暂不开放。
- 本轮补齐的是 Excel 中仍属于当前产品范围且此前证据不足的明细：商品详情、会员管理汇总、座台删除/禁用、库房角色权限、升盲规则序列。

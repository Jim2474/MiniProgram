# V63 验收记录

日期：2026-06-08

## 目标

继续补齐员工端存取酒操作的可追溯性：手工新增客户存酒和拒绝客户取酒申请时，员工可以填写真实原因。

## 已完成

- 员工端新增“新增存酒原因”输入。
- `/api/staff/storage` 提交时使用员工填写的 `storageReason`。
- 员工端新增“拒绝取酒原因”输入。
- `/api/staff/storage/pickup-requests/:requestId/reject` 提交时使用员工填写的 `pickupRejectReason`。
- 小程序结构校验新增员工端存酒原因和拒绝取酒原因检查，并禁止回退固定文案。

## 自动验收

```bash
npm test
```

通过项：

- 后端业务自验收：201 项。
- 小程序结构与 API 调用校验：166 项。

## 剩余风险

- 微信开发者工具模拟器仍需人工确认新增输入框在员工端长页面中的排版和输入体验。
- 取酒确认仍使用系统原因“员工确认取酒”；如后续门店需要每次确认也填写备注，可继续扩展。

# V38 验收记录

日期：2026-06-08

## 本轮目标

- 补齐 Excel 员工端“当月业绩每日增加、月底生成月数据”的展示细节。
- 保留原有近六个月历史业绩查询能力。

## 已完成

- 新增 `/api/staff/performance/daily`，支持按员工和月份统计当月每日销售额、订单数、提成率和提成金额。
- 每日业绩接口返回本月销售、订单和提成合计，并继续隐藏员工密码字段。
- 员工端新增“当月每日业绩”区块，展示本月合计和每天明细。
- 原“近六个月业绩”继续保留，用于历史月度查看。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：174 项通过。
- 小程序结构与 API 调用校验：134 项通过。

新增覆盖：

- 员工端返回当月每日业绩。
- 员工端返回当月每日提成合计。
- 员工页调用每日业绩接口并展示“当月每日业绩”。

## 剩余上线风险

- 微信开发者工具/真机仍需对员工端业绩长列表滚动体验做回归。
- 当前每日业绩以订单 `paidAt`/`createdAt` 的服务端日期聚合，跨时区部署前需统一门店营业日口径。

# V49 验收记录

日期：2026-06-08

## 本轮目标

- 补齐后台 SKU 编辑入口，不只支持新增和上下架。
- 保持库存数量变动必须走盘点/出入库流水，避免编辑 SKU 绕过库存账。

## 已完成

- SKU 列表新增“填入编辑”按钮。
- 选择 SKU 后回填分类、名称、规格、单位、描述、价格、成本、供应商、库存展示、预警库存和存酒有效期。
- 新增“保存 SKU”按钮。
- 保存 SKU 时 PATCH 分类、名称、规格、单位、价格、成本、供应商、预警库存、描述和存酒有效期。
- SKU 编辑不提交 `stockQty`，库存数量仍通过盘点和出入库申请流转。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：178 项通过。
- 小程序结构与 API 调用校验：148 项通过。

新增覆盖：

- 后台页支持 SKU 编辑表单。
- 后台 SKU 编辑表单不绕过库存流水修改库存数量。

## 剩余上线风险

- 微信开发者工具/真机仍需确认编辑后表单回填和保存按钮位置是否顺手。
- 后续可拆成新增 SKU 与编辑 SKU 两个独立折叠区，减少误操作。

# V47 验收记录

日期：2026-06-08

## 本轮目标

- 让后台咖位开台占用从固定演示金额升级为真实表单。
- 保持客户预约只做申请/后台管理，不引入复杂实时排台。

## 已完成

- 后台咖位管理新增“开台占用表单”。
- 支持填写开台消费金额和开台原因。
- 开台占用时使用表单值提交 `consumptionAmount` 和 `reason`。
- 移除小程序后台固定 `consumptionAmount: 388` 的演示式写法。

## 自动验收

```bash
npm test
```

结果：

- 后端业务自验收：178 项通过。
- 小程序结构与 API 调用校验：144 项通过。

新增覆盖：

- 后台页支持真实桌台开台消费金额输入。
- 后台页不再固定提交 388 元开台金额。

## 剩余上线风险

- 微信开发者工具/真机仍需确认开台金额输入、状态筛选和分页组合操作体验。
- 当前开台表单是全局表单，后续可按桌台行展开独立输入。

# V90 验收记录

日期：2026-06-08

## 目标

补齐 Excel 后台升盲设置第 39 行“显示北京时间、显示截止报名倒计时”的现场展示闭环。此前后台已有两个开关，荷官端只显示开关值，没有真正显示北京时间或倒计时信息。

## 已完成

- 荷官端游戏进行中页面在 `showBeijingTime` 开启时显示实时北京时间。
- 荷官端通过页面定时器每秒刷新北京时间，并在页面隐藏/卸载时清理定时器。
- 荷官端在 `showRegistrationCountdown` 开启且本局计时器已加载时显示报名倒计时。
- 报名倒计时当前复用本局计时器剩余时间，避免在未定义报名截止时间字段前引入不透明规则。
- 新增现场时间信息样式，使用固定标签尺寸减少布局跳动。
- 小程序结构校验覆盖北京时间、报名倒计时和现场时间样式。

## 自动验收

```bash
npm test
```

通过项：

- 后端业务自验收：232 项。
- 小程序结构与 API 调用校验：206 项。

## 剩余风险

- 若后续要做真正的“报名截止时间”配置，需要新增截止时间字段、后台配置入口和荷官端倒计时来源；本轮只把已有开关落实为现场可见信息。

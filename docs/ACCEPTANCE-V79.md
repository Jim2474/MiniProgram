# V79 验收记录

日期：2026-06-08

## 目标

收紧 V78 新增的公开订单支付信息。营业明细需要支付方式和支付时间，但不应把预支付签名字段、支付参数或 requestPayment 对象随订单列表扩散给后台列表接口。

## 已完成

- 新增 `publicPayment` 脱敏函数。
- 公开订单中的 `payment` 去除 `nonceStr`、`paySign`、`prepayId` 和 `requestPayment`。
- 营业明细仍保留 provider、状态、支付时间、交易号等审计字段。
- 支付接口本身仍可返回前端发起微信支付所需的 `prepay/requestPayment` 参数。
- 后端自验收覆盖营业明细支付记录不暴露预支付签名字段。

## 自动验收

```bash
npm test
```

通过项：

- 后端业务自验收：223 项。
- 小程序结构与 API 调用校验：182 项。

## 剩余风险

- 生产联调后可继续按微信支付字段敏感级别细分公开范围，例如平台证书序列号、退款流水号和商户单号展示策略。

# 德扑棋牌门店小程序 V2 PRD

## 1. 版本目标

V2 的目标是把 V1 Web 原型升级为真正可进入微信开发者工具运行的小程序项目，并将后端独立为可测试 API 服务。

V2 不再做压力测试，不改变 V1 已确认的核心业务边界，而是完成工程落地：

- 后端独立在 `backend/`，提供 REST API、业务规则、数据持久化和自动测试。
- 前端独立在 `miniprogram/`，采用微信小程序原生 WXML/WXSS/JS。
- 小程序覆盖客户、员工、商家后台、荷官四个入口。
- 本地开发时小程序调用 `http://localhost:3000` 后端。
- 必须完成自动测试，并尽量用微信开发者工具打开模拟器验证。

## 2. V2 必须保留的 V1 业务能力

### 2.1 客户端

- 商品分类与 SKU 商品展示。
- 员工二维码归属模拟：选择员工后进入点单上下文。
- 加入购物车、修改数量、删除商品。
- 创建订单。
- 模拟微信支付。
- 支付成功后订单进入待处理。
- 支付成功后扣减 SKU 库存。
- 支付成功后按每元 1 积分赠送。
- 查看我的订单。
- 查看积分余额与积分流水。
- 查看我的存酒。
- 提交取酒申请。
- 查看桌位/卡座并提交预约。

### 2.2 员工端

- 选择当前员工。
- 展示员工专属二维码信息。
- 查看个人订单和业绩。
- 手动新增客户存酒。
- 确认/拒绝客户取酒申请。
- 限额内手动调整客户积分。

### 2.3 商家后台

- 首页看板：今日营业额、订单数、会员数、卡座使用率、低库存、待处理订单。
- 订单列表。
- 标记订单完成。
- 订单退款，联动库存和积分回滚。
- 订单转存生成客户存酒。
- SKU 库存查看与调整。
- 客户存酒列表。
- 预约列表与确认预约。
- 操作日志。

### 2.4 荷官端

- 创建升盲游戏。
- 暂停/继续。
- 手动升盲/降盲。
- 淘汰/恢复玩家。
- 记录 +1 手代入。
- 重开本局。

## 3. V2 工程结构

```text
backend/
  package.json
  src/server.mjs
  scripts/selftest.mjs
  data/.gitkeep

miniprogram/
  project.config.json
  app.json
  app.js
  app.wxss
  sitemap.json
  utils/api.js
  pages/
    customer/
    staff/
    admin/
    dealer/

docs/
  ACCEPTANCE-V2.md
```

## 4. 后端要求

- 使用 Node.js 原生 HTTP 服务，不依赖外部 npm 包。
- 默认端口 `3000`。
- 数据文件默认写入 `backend/data/store.json`。
- 首次启动自动生成种子数据。
- 保留 V1 所有核心 API。
- 新增 API 文档可由 README 说明。
- 测试文件必须能独立启动临时服务并使用临时数据文件。
- 测试结束后清理临时数据。

## 5. 小程序要求

- 使用微信小程序原生框架，不使用 Web SPA 作为前端。
- `project.config.json` 指向 `miniprogram/`。
- 小程序请求后端统一通过 `utils/api.js`。
- 本地后端地址默认：`http://localhost:3000`。
- 小程序需提供四个 tab：
  - 客户点单
  - 员工端
  - 商家后台
  - 荷官升盲
- 页面要能在微信开发者工具模拟器中渲染和操作。
- 不使用复杂构建流程，便于直接导入微信开发者工具。

## 6. V2 验收标准

### 6.1 后端自动验收

运行：

```bash
cd backend
npm test
```

必须通过以下检查：

- 种子会员和 SKU 存在。
- 员工二维码归属订单。
- 购物车创建订单。
- 模拟支付后订单进入待处理。
- 支付后库存扣减。
- 支付后积分赠送。
- 后台看板统计营收和员工业绩。
- 订单转存生成客户存酒。
- 客户提交取酒申请。
- 员工确认取酒后扣减客户存酒。
- 客户提交预约。
- 后台确认预约后桌位变为预订。
- 荷官创建升盲游戏。
- 升盲、淘汰、代入动作正确。
- 库存不足被拦截。
- 退款后库存恢复、积分扣回。
- 关键操作日志存在。

### 6.2 小程序文件验收

必须存在：

- `miniprogram/project.config.json`
- `miniprogram/app.json`
- `miniprogram/app.js`
- `miniprogram/app.wxss`
- `miniprogram/utils/api.js`
- `miniprogram/pages/customer/customer.*`
- `miniprogram/pages/staff/staff.*`
- `miniprogram/pages/admin/admin.*`
- `miniprogram/pages/dealer/dealer.*`

### 6.3 小程序模拟验收

优先使用微信开发者工具打开 `miniprogram/` 目录，并检查：

- 项目可导入。
- 模拟器可启动。
- 四个 tab 可切换。
- 客户页可加载商品。
- 员工页可加载员工与取酒申请。
- 后台页可加载看板、订单、库存、日志。
- 荷官页可创建和操作升盲游戏。

如果当前机器无法通过 CLI 自动打开微信开发者工具，必须记录尝试路径、失败原因，并用静态文件检查和后端联调测试作为补充证据，但不能跳过自动测试。

## 7. 非目标

- 不接真实微信支付。
- 不接真实微信登录。
- 不引入数据库。
- 不做多商户 SaaS。
- 不做打印机、酒水券、抽奖、签到、充值余额。
- 不做复杂 UI 动效。

## 8. 完成定义

- 后端和小程序目录分离。
- V1 Web 原型可保留在 `legacy-web/` 或移除，但不能影响 V2 运行。
- 后端自动测试全部通过。
- 小程序文件结构完整。
- 尽力完成微信开发者工具模拟器打开/验证。
- 验收记录写入 `docs/ACCEPTANCE-V2.md`。
- 代码提交并推送到 GitHub。

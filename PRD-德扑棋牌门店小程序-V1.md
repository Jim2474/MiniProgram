# 德扑棋牌门店小程序 V1 PRD

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 德扑棋牌门店小程序 |
| 文档版本 | V1.0 |
| 目标客户 | 有德州扑克/棋牌活动，同时卖酒水、有员工销售提成需求的单店商家 |
| 产品形态 | 微信小程序客户端 + 微信小程序员工/荷官入口 + PC Web 商家后台 |
| 版本定位 | 单店部署 MVP，数据结构预留多门店/多商家扩展 |
| 原始需求来源 | `小程序进阶版C(1).xlsx` |
| 输出日期 | 2026-06-08 |

## 2. 一句话说明

为德扑/棋牌门店提供一套围绕现场酒水销售的微信小程序系统：员工用专属二维码引导客户点单，客户微信支付后订单自动归属员工、扣减商品库存、发放积分，商家后台统一管理订单、库存、客户存酒、预约、员工业绩，并提供荷官使用的轻量升盲工具。

## 3. 背景与问题

目标门店通常同时存在棋牌/德扑活动、酒水销售、员工服务、客户存酒、桌位预约、现场计时等流程。如果依赖人工记账、微信群、纸质存酒单或口头确认，容易出现以下问题：

- 客户点单链路不统一，员工销售归属容易争议。
- 商品库存与实际销售脱节，缺少可追溯流水。
- 客户存酒权益靠人工记录，查询、取酒、过期处理容易混乱。
- 老板难以及时查看营收、订单、员工业绩和库存变化。
- 预约桌位与现场状态靠人工同步，容易遗漏。
- 德扑升盲和现场人数/代入统计需要单独工具，和门店运营割裂。

## 4. 产品目标

V1 的第一目标是跑通门店现场经营闭环：

**员工出示专属点单二维码 -> 客户扫码点酒水/商品 -> 微信支付 -> 订单归属到该员工 -> 客户获得积分 -> 商品库存扣减 -> 商家后台看到订单、营收、员工业绩。**

V1 的第二目标是补齐该场景中强相关的运营能力：

- 两套库存：门店商品库存与客户存酒库存分账管理。
- 轻量预约：客户提交桌位/卡座预约，后台人工确认。
- 轻量升盲：荷官可独立使用升盲计时、淘汰、代入统计。
- 固定角色权限：老板/管理员、员工、库管、荷官。

## 5. 压力测试结论

### 5.1 结论

原始需求覆盖客户端、员工端、商家后台、库存、财务、抽奖、酒水券、积分、充值、升盲配置等大量模块。完整实现会变成大型定制系统，不适合作为第一版。V1 必须围绕“扫码点单 + 员工归属 + 库存 + 存酒 + 后台经营数据”收敛。

### 5.2 V1 核心假设

目标商家愿意为了“员工二维码点单、业绩归属、库存扣减、客户存酒、积分记录、桌位预约、轻量升盲”新增或切换一套微信小程序运营流程。

### 5.3 V1 验证指标

| 指标 | V1 验证标准 |
|---|---|
| 真实订单 | 试点门店 14 天内完成不少于 30 笔客户自助扫码支付订单 |
| 员工归属 | 不少于 80% 的支付订单能归属到具体员工或明确为自然订单 |
| 库存准确性 | 支付、退款、手动调整均有库存流水，后台库存可追溯 |
| 存酒使用 | 试点期间至少完成 10 条客户存酒或取酒记录 |
| 后台可用性 | 老板能通过后台查看今日营收、订单、员工业绩、库存变化 |

## 6. V1 范围

### 6.1 V1 包含

- 微信登录与手机号绑定。
- 商品分类、SKU 商品、购物车、多商品合并微信支付。
- 员工专属二维码与订单归属。
- 订单管理、订单状态、订单通知、退款/取消。
- 消费赠送积分、积分明细、后台/员工手动增减积分。
- 门店商品库存：SKU 库存、库存流水、销售扣减、退款恢复、手动入库/出库/调整。
- 客户存酒库存：订单转存、手动新增、取酒申请、有效期、过期人工处理、存酒流水。
- 桌位/卡座后台管理与客户预约申请。
- 员工端：二维码、个人业绩、订单查看、存取酒处理、积分操作。
- 荷官端：轻量升盲系统。
- PC Web 商家后台：订单、商品、库存、会员、积分、存酒、预约、员工、数据看板、操作日志。
- 固定角色权限：老板/管理员、员工、库管、荷官。

### 6.2 V1 不包含

- 充值余额、储值卡、余额支付、充值赠送。
- 酒水券。
- 积分兑换、积分抽奖、积分排行榜。
- 签到送积分、补签、连续签到奖励。
- 员工代客户下单。
- 多商家 SaaS、平台总后台、商户入驻、套餐计费。
- 小票打印机、云打印、叫号屏。
- 复杂套餐/组合商品自动拆库存。
- 多仓库、供应商采购、批次成本核算。
- 复杂自动排台、订金、桌位时段冲突算法。
- 升盲系统皮肤、背景图、Logo、字体、语音文案、冠军图等高级自定义。
- 自动提成结算，只统计销售额与订单数。

## 7. V1 默认业务规则

| 事项 | V1 默认规则 |
|---|---|
| 积分赠送 | 默认每实付 1 元赠 1 积分，后台可配置全局比例；退款时扣回本单赠送积分 |
| 订单超时 | 待支付订单 15 分钟未支付自动关闭 |
| 库存扣减 | 下单前校验库存，微信支付成功后扣减库存；库存不足不可下单 |
| 库存预警 | 每个 SKU 可配置预警阈值，库存小于等于阈值时后台标记低库存 |
| 退款方式 | V1 支持后台管理员发起微信支付原路退款；退款成功后联动库存与积分回滚 |
| 存酒有效期 | 按 SKU 或存酒品类配置默认有效期；员工新增时可由管理员权限修改到期日 |
| 存酒过期 | 到期后标记为已过期，不自动作废；管理员可延期、作废、恢复可取 |
| 存酒协议 | V1 在新增存酒时展示并勾选简版寄存协议，协议文本后台可配置 |
| 员工积分权限 | 员工可手动增减积分，但单次超过后台配置阈值需管理员操作 |
| 订单处理 | 支付成功后订单进入待处理；吧台/指定处理角色标记已完成 |
| 订单归属调整 | 管理员可调整订单归属，必须填写原因并写入操作日志 |
| 数据结构 | 单店部署，但核心表预留 `merchant_id` 与 `store_id` |

## 8. 用户角色与权限

| 模块/能力 | 客户 | 员工 | 荷官 | 库管 | 老板/管理员 |
|---|---:|---:|---:|---:|---:|
| 浏览商品/桌位 | 是 | 是 | 是 | 是 | 是 |
| 客户自助下单支付 | 是 | 否 | 否 | 否 | 否 |
| 展示员工二维码 | 否 | 是 | 可选 | 否 | 是 |
| 查看本人订单/积分/存酒 | 是 | 否 | 否 | 否 | 是 |
| 查看个人员工业绩 | 否 | 是 | 否 | 否 | 是 |
| 查看全部订单 | 否 | 否 | 否 | 可看待处理 | 是 |
| 标记订单完成 | 否 | 可配置 | 否 | 是 | 是 |
| 发起退款 | 否 | 否 | 否 | 否 | 是 |
| 商品与 SKU 管理 | 否 | 否 | 否 | 可查看 | 是 |
| 门店商品库存调整 | 否 | 否 | 否 | 是 | 是 |
| 客户存酒新增/处理 | 申请取酒 | 是 | 否 | 是 | 是 |
| 积分手动增减 | 否 | 限额内 | 否 | 否 | 是 |
| 桌位预约处理 | 提交申请 | 可配置 | 否 | 否 | 是 |
| 升盲系统 | 否 | 可选 | 是 | 否 | 是 |
| 员工管理 | 否 | 否 | 否 | 否 | 是 |
| 操作日志查看 | 否 | 否 | 否 | 本人相关 | 是 |

## 9. 端与页面清单

### 9.1 微信小程序客户侧

| 页面 | 主要内容 |
|---|---|
| 首页 | 门店信息、商品入口、预约入口、我的入口 |
| 商品分类页 | 分类列表、商品列表、库存状态 |
| 商品详情页 | 图片、名称、规格、价格、库存、描述、加入购物车 |
| 购物车 | 商品明细、数量调整、库存提示、合计金额、提交订单 |
| 订单确认页 | 商品明细、员工归属提示、实付金额、积分预计赠送 |
| 支付结果页 | 支付成功/失败、订单状态、返回订单 |
| 我的订单 | 今日订单、历史订单、订单详情 |
| 我的积分 | 积分余额、积分明细 |
| 我的存酒 | 存酒列表、有效期、状态、取酒申请 |
| 取酒申请页 | 选择存酒记录、取酒数量、提交申请 |
| 桌位预约页 | 桌位/卡座列表、图片、人数、状态、预约申请 |
| 个人中心 | 登录状态、头像昵称、手机号、积分、存酒、订单、客服、门店位置 |

### 9.2 微信小程序员工/荷官侧

| 页面 | 主要内容 |
|---|---|
| 员工登录 | 账号密码登录 |
| 员工首页 | 今日个人销售额、订单数、二维码入口、待处理事项 |
| 专属二维码 | 员工个人二维码展示 |
| 我的订单 | 本人归属订单、新订单提示、订单详情 |
| 个人业绩 | 当月销售额、订单数、商品明细、历史 6 个月查询 |
| 客户查询 | 按手机号/客户码查询客户 |
| 存酒处理 | 新增存酒、确认取酒、拒绝取酒、存酒流水 |
| 积分处理 | 客户积分查询、限额内手动增减、原因填写 |
| 修改密码 | 员工修改登录密码 |
| 升盲设置 | 初始盲注、升盲间隔、初始人数、单手买入金额、语音开关 |
| 升盲进行中 | 倒计时、当前盲注、暂停/继续、升盲/降盲、淘汰/恢复、代入统计 |

### 9.3 PC Web 商家后台

| 页面 | 主要内容 |
|---|---|
| 登录页 | 管理员/库管账号密码登录 |
| 首页看板 | 今日营业额、订单数、新增会员、卡座使用率、近 7 日营收、最新订单、员工业绩排行 |
| 订单管理 | 订单列表、筛选、详情、标记完成、调整归属、退款 |
| 商品分类 | 分类新增、编辑、禁用、排序 |
| SKU 商品 | 商品新增、编辑、上下架、规格、单位、价格、库存、预警阈值 |
| 门店库存 | SKU 库存列表、入库、出库、调整、库存流水 |
| 客户存酒 | 存酒列表、新增存酒、订单转存、取酒申请处理、延期、作废、恢复、流水 |
| 会员管理 | 会员列表、会员详情、订单、积分、存酒 |
| 积分管理 | 积分规则、积分明细、手动增减、异常回滚记录 |
| 桌位管理 | 桌位/卡座新增、编辑、图片、类型、人数、状态 |
| 预约管理 | 预约申请、确认、取消、失效处理 |
| 员工管理 | 员工新增、编辑、禁用、角色分配、密码重置 |
| 业绩统计 | 员工销售额、订单数、商品明细、时间筛选、导出 |
| 升盲记录 | 可选查看游戏记录、代入统计记录 |
| 操作日志 | 关键操作日志查询、筛选、详情 |
| 系统设置 | 门店信息、积分比例、订单超时时间、存酒协议、权限阈值 |

## 10. 核心业务流程

### 10.1 员工二维码点单流程

1. 员工登录员工端，展示个人专属二维码。
2. 客户扫码进入小程序，系统记录 `employee_id` 到当前下单上下文。
3. 客户浏览商品，加入购物车。
4. 客户提交订单，系统校验 SKU 库存。
5. 库存充足时创建待支付订单，锁定订单归属员工。
6. 客户调起微信支付。
7. 微信支付回调成功后：
   - 订单状态变为待处理。
   - 写入支付记录。
   - 扣减 SKU 库存并写入库存流水。
   - 按规则赠送积分并写入积分流水。
   - 员工业绩统计增加。
   - 员工端/后台出现新订单提示。
8. 吧台/员工/管理员完成出品后标记订单已完成。

### 10.2 自然订单流程

1. 客户从小程序首页直接进入。
2. 未携带员工二维码参数时，下单来源为自然订单。
3. 支付、库存、积分、订单处理逻辑与员工二维码订单一致。
4. 后台员工业绩不计入具体员工，可单独统计自然订单。

### 10.3 退款/取消流程

1. 待支付订单超过 15 分钟未支付，系统自动关闭。
2. 已支付订单由管理员在后台发起退款。
3. 管理员填写退款原因。
4. 微信支付原路退款成功后：
   - 订单状态变为已取消/退款。
   - 恢复本单扣减的 SKU 库存。
   - 扣回本单赠送积分。
   - 如果客户积分不足扣回，生成积分异常记录。
   - 写入退款记录、库存流水、积分流水、操作日志。

### 10.4 订单转存流程

1. 客户完成支付后，如部分酒水未消费，可由员工或管理员发起订单转存。
2. 选择订单内可转存的酒水 SKU 与数量。
3. 系统生成客户存酒记录，自动计算有效期。
4. 订单转存写入客户存酒流水和操作日志。
5. 客户可在“我的存酒”查看。

### 10.5 手动新增存酒流程

1. 员工/库管/管理员输入客户手机号或选择会员。
2. 选择酒水 SKU、数量、有效期、备注。
3. 勾选寄存协议确认。
4. 系统生成客户存酒记录。
5. 写入客户存酒流水和操作日志。

### 10.6 取酒流程

1. 客户在“我的存酒”选择存酒记录并提交取酒数量。
2. 取酒申请状态变为待确认。
3. 员工端通过手机号或客户码查询待确认申请。
4. 员工确认取酒后，扣减客户存酒库存。
5. 如拒绝/取消，员工必须填写原因。
6. 已过期存酒不可直接取酒，需管理员延期或恢复可取后再处理。

### 10.7 桌位预约流程

1. 客户进入桌位预约页，查看桌位/卡座信息。
2. 客户提交预约申请。
3. 后台生成待确认预约。
4. 管理员人工确认或取消。
5. 确认后桌位状态可标记为预订。
6. 到店后员工/管理员可标记为占用；结束后恢复空闲。

### 10.8 升盲流程

1. 荷官进入升盲设置页，设置初始小盲/大盲、升盲间隔、初始人数、单手买入金额。
2. 点击开始进入游戏进行中页面。
3. 系统倒计时并在 30 秒、10 秒、升盲时默认语音提醒。
4. 荷官可暂停/继续、手动升盲/降盲、重新开始。
5. 荷官可淘汰座位或撤销淘汰，当前人数自动变化。
6. 荷官可记录代入手数，系统计算总代入金额。

## 11. 状态定义

### 11.1 订单状态

| 状态 | 说明 | 可流转到 |
|---|---|---|
| 待支付 | 已创建订单，未完成支付 | 待处理、已关闭 |
| 已关闭 | 未支付超时或客户取消 | 终态 |
| 待处理 | 已支付，等待门店出品/处理 | 已完成、已取消/退款 |
| 已完成 | 门店已完成出品/服务 | 已取消/退款 |
| 已取消/退款 | 管理员退款成功或取消 | 终态 |

### 11.2 支付状态

| 状态 | 说明 |
|---|---|
| 未支付 | 未调起或未完成支付 |
| 支付中 | 已调起微信支付，等待回调 |
| 已支付 | 微信支付成功 |
| 已退款 | 原路退款成功 |
| 支付失败 | 支付失败或回调异常 |

### 11.3 客户存酒状态

| 状态 | 说明 |
|---|---|
| 可取 | 未过期且数量大于 0 |
| 待取酒确认 | 客户已提交取酒申请 |
| 已取完 | 数量为 0 |
| 已过期 | 到期后系统标记 |
| 已作废 | 管理员人工作废 |

### 11.4 预约状态

| 状态 | 说明 |
|---|---|
| 待确认 | 客户提交预约，后台未处理 |
| 已确认 | 后台确认预约 |
| 已取消 | 客户或后台取消 |
| 已失效 | 预约时间已过或后台设为失效 |

### 11.5 桌位状态

| 状态 | 说明 |
|---|---|
| 空闲 | 可预约/可使用 |
| 占用 | 当前正在使用 |
| 预订 | 已确认预约 |
| 维护 | 暂不可用 |
| 禁用 | 后台停用 |

## 12. 功能需求详情

### 12.1 客户登录与会员

- 游客可浏览商品与桌位。
- 下单、预约、查看积分、查看存酒、申请取酒时必须微信登录。
- 首次支付或使用会员能力时引导手机号授权。
- 会员主标识为微信 `openid`，手机号用于线下识别和后台搜索。
- 后台支持按手机号、昵称、会员 ID 搜索。
- 不支持客户账号密码登录。

### 12.2 商品与购物车

- 商品必须归属分类。
- SKU 字段包括商品名、分类、规格、单位、售价、库存数量、预警阈值、上下架状态、描述、图片。
- 客户端仅展示上架商品。
- 商品库存为 0 或不足时，客户端应明确提示不可购买或数量不足。
- 支持购物车多商品合并支付。
- V1 不做复杂组合套餐；套餐可作为普通 SKU 管理。

### 12.3 微信支付

- 使用微信小程序支付。
- 支付成功以微信支付回调为准。
- 支付回调必须具备幂等处理，避免重复扣库存或重复赠送积分。
- 后台订单详情展示微信交易号、支付时间、支付金额。
- 退款由管理员从后台发起原路退款。

### 12.4 员工二维码归属

- 每个员工拥有唯一二维码。
- 二维码携带员工归属参数。
- 订单创建时固化员工归属，不因客户后续切换页面丢失。
- 客户不能手动选择员工。
- 管理员可调整订单归属，必须填写原因。

### 12.5 积分

- 默认每实付 1 元赠送 1 积分，后台可配置全局比例。
- 赠送积分按订单实付金额计算，不含退款金额。
- 积分流水包括消费赠送、手动增加、手动减少、退款扣回、异常记录。
- 员工手动增减积分受后台配置阈值限制。
- 所有手动积分变更必须填写原因。
- V1 不做积分兑换、抽奖、排行榜、签到。

### 12.6 门店商品库存

- 门店商品库存按 SKU 管理。
- 下单提交前校验库存。
- 支付成功后扣减库存。
- 退款成功后恢复库存。
- 手动入库、出库、调整均生成库存流水。
- 库存数量以流水结果和 SKU 当前库存保持一致。
- 库存低于预警阈值时后台标记低库存。
- V1 不做多仓库、供应商采购、批次成本。

### 12.7 客户存酒库存

- 客户存酒库存与门店商品库存分成两套账。
- 客户存酒代表客户权益，不等同于门店可售库存。
- 存酒来源包括订单转存、员工/后台手动新增。
- 取酒确认后扣减客户存酒数量。
- 支持按 SKU 或品类配置默认有效期。
- 过期后标记为已过期，不自动作废。
- 管理员可延期、作废、恢复可取。
- 新增存酒时需勾选寄存协议，协议文本后台可配置。

### 12.8 桌位/卡座

- 后台配置桌位/卡座名称、类型、容纳人数、图片、状态。
- 客户端展示可见桌位/卡座。
- 客户可提交预约申请。
- 后台人工确认/取消预约。
- 员工或管理员可维护桌位状态。
- V1 不做订金、自动排台、复杂时段冲突算法。

### 12.9 订单通知

- 支付成功后员工端/后台订单列表出现新订单。
- 员工二维码产生的订单，员工本人可见。
- 管理员/老板可见全部订单。
- 吧台/指定处理角色可见全部待处理订单。
- V1 先做小程序内订单列表刷新与 PC 后台新订单提示。
- V1 不接小票打印机、云打印、叫号屏，不做复杂微信订阅消息。

### 12.10 PC 后台导出

- V1 支持基础 CSV/Excel 导出：订单列表、员工业绩、库存流水、客户存酒流水、积分流水。
- 导出受角色权限控制。

## 13. 数据实体初稿

| 实体 | 关键字段 |
|---|---|
| Merchant | merchant_id, name, status, created_at |
| Store | store_id, merchant_id, name, address, phone, logo_url, status |
| User | user_id, merchant_id, store_id, openid, nickname, avatar, phone, points_balance, created_at |
| Employee | employee_id, merchant_id, store_id, name, phone, role, login_account, password_hash, status, created_at |
| Category | category_id, merchant_id, store_id, name, sort_order, status |
| ProductSKU | sku_id, merchant_id, store_id, category_id, name, spec, unit, price, stock_qty, warning_qty, status, description, image_url |
| Cart | cart_id, user_id, employee_id, source, created_at |
| CartItem | cart_item_id, cart_id, sku_id, quantity |
| Order | order_id, merchant_id, store_id, user_id, employee_id, source, amount, pay_status, order_status, points_awarded, created_at |
| OrderItem | order_item_id, order_id, sku_id, name_snapshot, spec_snapshot, unit_snapshot, price, quantity, subtotal |
| Payment | payment_id, order_id, wx_transaction_id, amount, status, paid_at, refunded_at |
| Refund | refund_id, order_id, amount, reason, status, operator_id, created_at |
| PointsLedger | ledger_id, merchant_id, store_id, user_id, change_amount, balance_after, reason, source_type, source_id, operator_id, created_at |
| StockLedger | ledger_id, merchant_id, store_id, sku_id, change_qty, stock_after, change_type, source_type, source_id, operator_id, reason, created_at |
| CustomerStorage | storage_id, merchant_id, store_id, user_id, sku_id, quantity, expire_at, status, created_at |
| CustomerStorageLedger | ledger_id, storage_id, user_id, sku_id, change_qty, quantity_after, action_type, operator_id, reason, created_at |
| StoragePickupRequest | request_id, storage_id, user_id, quantity, status, reason, handled_by, created_at, handled_at |
| Reservation | reservation_id, merchant_id, store_id, user_id, table_id, status, reservation_time, contact_phone, remark, created_at |
| TableSeat | table_id, merchant_id, store_id, name, type, capacity, image_url, status |
| BlindGame | game_id, merchant_id, store_id, operator_id, small_blind, big_blind, interval_minutes, initial_players, current_players, buyin_amount, buyin_count, status, created_at |
| OperationLog | log_id, merchant_id, store_id, operator_id, role, action, target_type, target_id, before_json, after_json, reason, created_at |
| SystemSetting | setting_id, merchant_id, store_id, key, value, updated_by, updated_at |

## 14. API 草案

### 14.1 客户端 API

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/wechat/login` | 微信登录，返回用户会话 |
| POST | `/api/user/bind-phone` | 绑定手机号 |
| GET | `/api/products/categories` | 商品分类 |
| GET | `/api/products` | 商品列表 |
| GET | `/api/products/{skuId}` | 商品详情 |
| POST | `/api/cart/items` | 加入购物车 |
| PATCH | `/api/cart/items/{itemId}` | 修改购物车数量 |
| DELETE | `/api/cart/items/{itemId}` | 删除购物车商品 |
| POST | `/api/orders` | 创建订单 |
| POST | `/api/orders/{orderId}/pay` | 创建微信支付参数 |
| GET | `/api/orders` | 我的订单 |
| GET | `/api/points` | 我的积分余额与明细 |
| GET | `/api/storage` | 我的存酒 |
| POST | `/api/storage/{storageId}/pickup-requests` | 提交取酒申请 |
| GET | `/api/tables` | 桌位/卡座列表 |
| POST | `/api/reservations` | 提交预约 |

### 14.2 员工端 API

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/staff/login` | 员工登录 |
| GET | `/api/staff/qrcode` | 获取专属二维码 |
| GET | `/api/staff/orders` | 员工订单 |
| GET | `/api/staff/performance` | 个人业绩 |
| GET | `/api/staff/users/search` | 按手机号查询客户 |
| POST | `/api/staff/storage` | 手动新增存酒 |
| POST | `/api/staff/storage/pickup-requests/{requestId}/confirm` | 确认取酒 |
| POST | `/api/staff/storage/pickup-requests/{requestId}/reject` | 拒绝取酒 |
| POST | `/api/staff/points/adjust` | 员工限额内调整积分 |
| POST | `/api/staff/blind-games` | 创建升盲游戏 |
| PATCH | `/api/staff/blind-games/{gameId}` | 更新升盲游戏状态 |

### 14.3 后台 API

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/admin/login` | 后台登录 |
| GET | `/api/admin/dashboard` | 首页看板 |
| GET/POST | `/api/admin/categories` | 分类查询/新增 |
| GET/POST | `/api/admin/products` | SKU 查询/新增 |
| PATCH | `/api/admin/products/{skuId}` | SKU 编辑/上下架 |
| GET | `/api/admin/orders` | 订单查询 |
| PATCH | `/api/admin/orders/{orderId}/complete` | 标记完成 |
| PATCH | `/api/admin/orders/{orderId}/employee` | 调整订单归属 |
| POST | `/api/admin/orders/{orderId}/refund` | 退款 |
| GET | `/api/admin/stock-ledgers` | 库存流水 |
| POST | `/api/admin/stock/inbound` | 入库 |
| POST | `/api/admin/stock/outbound` | 出库 |
| POST | `/api/admin/stock/adjust` | 库存调整 |
| GET/POST | `/api/admin/customer-storage` | 客户存酒查询/新增 |
| POST | `/api/admin/customer-storage/from-order` | 订单转存 |
| PATCH | `/api/admin/customer-storage/{storageId}` | 延期/作废/恢复 |
| GET | `/api/admin/users` | 会员列表 |
| GET | `/api/admin/points-ledgers` | 积分明细 |
| POST | `/api/admin/points/adjust` | 管理员调整积分 |
| GET/POST | `/api/admin/tables` | 桌位查询/新增 |
| PATCH | `/api/admin/tables/{tableId}` | 桌位编辑/状态变更 |
| GET | `/api/admin/reservations` | 预约列表 |
| PATCH | `/api/admin/reservations/{reservationId}` | 预约确认/取消/失效 |
| GET/POST | `/api/admin/employees` | 员工查询/新增 |
| PATCH | `/api/admin/employees/{employeeId}` | 员工编辑/禁用/角色 |
| POST | `/api/admin/employees/{employeeId}/reset-password` | 重置密码 |
| GET | `/api/admin/operation-logs` | 操作日志 |
| GET | `/api/admin/export/{type}` | 导出订单/业绩/流水 |

## 15. 非功能需求

### 15.1 安全

- 客户端使用微信登录态识别用户。
- 员工端和后台账号密码必须加密存储，不保存明文密码。
- 后台接口必须校验角色权限。
- 支付回调、退款回调必须验签。
- 关键操作必须写操作日志。

### 15.2 一致性

- 支付回调必须幂等。
- 库存扣减、积分赠送、订单状态变更应在同一事务或可补偿流程中完成。
- 退款回滚库存和积分必须可追溯。
- 库存当前值与库存流水应可对账。

### 15.3 可用性

- 客户端核心路径尽量控制在 4 步内：扫码 -> 加购 -> 支付 -> 成功。
- 员工二维码页面应支持快速打开。
- 后台列表支持分页、搜索、筛选。
- 低库存、取酒待确认、待处理订单应有明显状态提示。

### 15.4 性能

- 商品列表、订单列表、库存列表均分页加载。
- 首页看板按日统计数据可缓存。
- 支付成功后的订单状态更新应尽快反映到员工端和后台。

## 16. 验收标准

### 16.1 核心点单闭环

- 员工可展示专属二维码。
- 客户扫码后可浏览商品、加入购物车、微信支付。
- 支付成功后订单归属到该员工。
- 支付成功后自动扣减商品 SKU 库存。
- 支付成功后自动赠送积分。
- 后台可查看该订单、营收、员工销售数据。

### 16.2 订单与退款

- 待支付订单超过 15 分钟自动关闭。
- 已支付订单仅管理员可退款。
- 退款成功后订单状态、库存、积分均正确回滚。
- 积分不足扣回时生成异常记录。
- 退款操作写入操作日志。

### 16.3 库存

- 库存不足时客户无法提交订单。
- 支付成功订单必须生成库存扣减流水。
- 退款成功必须生成库存恢复流水。
- 后台手动调整库存必须记录原因和操作人。
- 客户存酒与门店商品库存分开管理。

### 16.4 存酒

- 已支付订单中的酒水可转为客户存酒。
- 员工/后台可手动新增客户存酒。
- 客户可查看存酒并提交取酒申请。
- 员工确认取酒后客户存酒数量减少。
- 过期存酒不可直接取酒，需管理员处理。

### 16.5 积分

- 消费后按配置规则赠送积分。
- 客户可查看积分余额和明细。
- 后台/员工可手动增减积分并记录原因。
- 员工超过限额的积分调整不可提交。
- 退款成功后扣回本次赠送积分。

### 16.6 后台

- 管理员可管理商品、库存、订单、会员、存酒、预约、员工。
- 管理员可查看今日营收、今日订单、近 7 日趋势、员工业绩。
- 后台可导出订单、员工业绩、库存流水、客户存酒流水、积分流水。
- 所有关键变更写入操作日志。

### 16.7 升盲

- 荷官可设置初始盲注、升盲间隔、初始人数、单手买入金额。
- 倒计时支持开始、暂停、继续、升盲、降盲、重开。
- 支持淘汰座位和撤销淘汰。
- 支持代入手数和总代入金额统计。
- 默认语音提醒在 30 秒、10 秒、升盲时触发。

## 17. 里程碑建议

| 阶段 | 周期 | 交付内容 |
|---|---|---|
| M1 基础框架 | 第 1 周 | 小程序/后台项目框架、登录、角色、门店配置、基础数据模型 |
| M2 商品点单 | 第 2-3 周 | 分类、SKU、购物车、微信支付、订单、员工二维码归属 |
| M3 库存与积分 | 第 4 周 | 商品库存流水、库存扣减/恢复、积分赠送/明细/手动调整 |
| M4 存酒与预约 | 第 5 周 | 客户存酒、取酒申请、有效期、桌位/预约管理 |
| M5 后台经营数据 | 第 6 周 | 首页看板、员工业绩、导出、操作日志 |
| M6 升盲与联调 | 第 7 周 | 轻量升盲、端到端联调、异常场景、试点准备 |
| M7 试点修正 | 第 8 周 | 门店试跑、问题修复、验收与上线 |

## 18. 二期候选功能

- 充值余额、充值赠送、余额支付。
- 酒水券、积分兑换、积分抽奖、排行榜。
- 签到送积分、会员等级。
- 复杂套餐与组合商品拆库存。
- 多仓库、供应商采购、批次成本核算。
- 小票打印机、云打印、叫号屏。
- 微信订阅消息。
- 多门店/多商家 SaaS、平台总后台。
- 升盲系统皮肤、背景、Logo、字体、语音文案、冠军图自定义。
- 自动提成结算。

## 19. 当前版本假设与可调整项

以下规则已经作为 V1 默认口径写入本 PRD，后续可以在设计或开发前微调，但不阻塞 V1 启动：

- 积分默认每实付 1 元赠 1 积分。
- 待支付订单 15 分钟自动关闭。
- 后台支持微信支付原路退款。
- SKU 支持独立库存预警阈值。
- 新增存酒时需要勾选简版寄存协议。
- 员工积分调整受后台阈值限制。
- 吧台处理权限可由员工或库管角色配置承担。
- PC 后台支持基础数据导出。
